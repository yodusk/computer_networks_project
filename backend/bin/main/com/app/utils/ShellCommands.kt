package com.app.utils

class ShellCommands {
  companion object {
    const val cd: String = "cd"
    const val rm = "rm -rf"
    const val curl = "curl"
    const val unzip = "unzip"
    const val mv = "mv"
    const val mkdir = "mkdir"
  }
}

class Shell {
  companion object {
    const val workingDir: String = "working"
  }
}
